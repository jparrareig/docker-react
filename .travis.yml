language: generic 
sudo: required
services:
    - docker
    
before_install:
    - docker build -e CI=true -t jparrareig/docker-react -f Dockerfile.dev . 

script:
    - docker run jparrareig/docker-react npm run test -- --coverage
